function foo(a,b){    if(a === 0){        return b;    }else if(b === 0){        return a;    }else{        return foo(a-1,b-1);    } }console.log(foo(5,5)); // this will work fine.console.log(foo(5,0)); // this will also work fine.console.log(foo(0,5)); // this will also work fine.console.log(foo(0,0)); // this will also work fine.console.log(foo(-1,5)); // this will cause a stack overflow error because the recursion never ends. 