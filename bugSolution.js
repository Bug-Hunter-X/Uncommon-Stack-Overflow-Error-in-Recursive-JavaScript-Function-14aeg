function foo(a, b) {  if (a < 0 || b < 0) {    // Handle negative inputs appropriately.  For example, throw an error or return a default value.    throw new Error("Inputs must be non-negative.");  }  if (a === 0) {    return b;  } else if (b === 0) {    return a;  } else {    return foo(a - 1, b - 1);  } }console.log(foo(5, 5)); // Output: 0console.log(foo(5, 0)); // Output: 5console.log(foo(0, 5)); // Output: 5console.log(foo(0, 0)); // Output: 0console.log(foo(-1, 5)); // Throws an error: "Inputs must be non-negative." 